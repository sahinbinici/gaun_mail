package gaun.apply.enums;

public enum ApplicationStatusEnum {
    PENDING,      // Application is submitted but not reviewed
    APPROVED,     // Application is approved
    REJECTED,     // Application is rejected
    IN_REVIEW     // Application is being reviewed
}
